<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.avorobyev.poorchild">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>
    <uses-permission android:name="android.permission.GET_TASKS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".Services.CloseOtherAppsService"/>
        <service android:name=".Services.LockScreenService"/>

        <receiver
            android:name=".Receivers.BootCompletedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Declaring broadcast receiver for BOOT_COMPLETED event. -->

        <activity android:name=".Child.ListOfWorksActivity">
        </activity>
        <activity android:name=".Child.ViewDeviceCodeForAddToParentActivity">
        </activity>
        <activity android:name=".Parent.AddChildDeviceActivity">
        </activity>
        <activity android:name=".Parent.ListOfWorksActivity">
        </activity>
        <activity android:name=".Parent.AddWorkActivity">
        </activity>
    </application>

</manifest>